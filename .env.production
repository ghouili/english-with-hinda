# Production build config (baked into the client bundle by `vite build`).
# VITE_API_URL is a public value — it ends up in the shipped JS — so it is safe
# to commit. Points the app at the dedicated API host. No trailing slash.
VITE_API_URL=https://api.learnenglish.tn
